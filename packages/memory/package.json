{
  "name": "@doc-assistant/memory",
  "version": "0.6.0-beta.2",
  "private": true,
  "description": "Doc Assistant · 记忆层（v0.2 Dexie 落地：四层记忆 + 向量召回）",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@doc-assistant/shared": "workspace:*",
    "dexie": "^4.0.10"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "vitest": "^2.1.4"
  }
}
