{
  "name": "@doc-assistant/memory",
  "version": "0.1.0",
  "private": true,
  "description": "Doc Assistant · 记忆层（MVP 仅接口 + NullMemoryStore）",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "@doc-assistant/shared": "workspace:*"
  }
}
