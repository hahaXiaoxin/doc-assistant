/**
 * @doc-assistant/memory · 入口
 * ---------------------------------------------
 * 职责：长期记忆存储与召回（remember / recall）。
 *
 * MVP 范围：仅定义接口 + NullMemoryStore 空实现；不安装 dexie。
 * PHASE2: Dexie 落地 + 千问 embedding + 向量检索，详见 docs/ROADMAP.md §2。
 */

export {};
