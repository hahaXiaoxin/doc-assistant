{
  "name": "@doc-assistant/provider",
  "version": "0.5.0",
  "private": true,
  "description": "Doc Assistant · Provider 层（LLM 协议适配）",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@doc-assistant/shared": "workspace:*",
    "@ai-sdk/openai": "^1.3.23",
    "ai": "^4.3.16",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  }
}
