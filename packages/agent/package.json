{
  "name": "@doc-assistant/agent",
  "version": "0.3.0",
  "private": true,
  "description": "Doc Assistant · Agent 层（多 Agent 编排 + ContextSource 抽象）",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "@doc-assistant/shared": "workspace:*",
    "@doc-assistant/provider": "workspace:*",
    "@doc-assistant/tools": "workspace:*",
    "@doc-assistant/memory": "workspace:*"
  }
}
