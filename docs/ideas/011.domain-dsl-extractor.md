# 域名级 DSL 自学习文章提取器

- **ID**: ideas-011
- **状态**: proposed
- **优先级**: P1(版本主题:v0.6 / Phase 2-b)
- **来源**: `docs/ROADMAP.md` · §1
- **创建日期**: 2026-05-24

## 背景
不同站点 DOM 差异巨大(Notion / Figma / Canvas 渲染文档等),Readability + 语义化策略均有盲区。每个长尾域名手写 extractor 不现实。

## 提案
首次访问某域名时,LLM 基于页面样本自动推导一份**可复用的 JSON DSL 配置**,存档后该域名永远零 LLM 成本提取。
- **核心红线**:LLM 生成 DSL 不生成 JS;严禁 `eval` / `new Function` / 动态 `import`;DSL 只能调用白名单工具。
- **DSL Schema**(草案):
  ```ts
  interface DomainExtractorScript {
    version: 1;
    domain: string;
    urlPattern?: string;
    createdAt: number;
    lastValidatedAt: number;
    identity: { idFrom: IdentitySource[]; titleFrom: IdentitySource[] };
    content: { root: Selector; exclude: Selector[]; titleSelector?: Selector; format: 'text' | 'markdown' };
  }
  ```
- **解释器约束**:CSS 选择器 sanitize / 不访问 `cookie` / `localStorage` / `fetch` / 不修改 DOM(只读 + cloneNode)/ 500ms 超时降级。

## 验收标准
- [ ] DSL 解释器 + 白名单工具实装。
- [ ] 注册到 `identityRegistry` / `contentRegistry`(已留扩展位 priority=100+)。
- [ ] LLM 生成 → 持久化 → 解释器执行 → 产物匹配预期。
- [ ] 三大反例覆盖:`eval` 路径被拒 / 网络访问被拒 / 超时降级。

## 备注
- 已在 MVP 中预留扩展接缝(`packages/tools/src/registry.ts`、`page/identity` `page/content`)。
- 与 ideas-009(WebFetch) 强相关 — DSL 提取器是 WebFetch 天然的 HTML 清洗层,优先被 ideas-009 复用。
